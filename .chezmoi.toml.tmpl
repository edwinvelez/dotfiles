{{- $hostname := .chezmoi.hostname -}}
{{- $is_desktop := false -}}
{{- $is_laptop := false -}}

{{- if eq $hostname "my-desktop" -}}
{{-   $is_desktop = true -}}
{{- else if eq $hostname "my-laptop" -}}
{{-   $is_laptop = true -}}
{{- end -}}

sourceDir = "{{ .chezmoi.homeDir }}/Dropbox/dotfiles"

[data]
    hostname = {{ $hostname | quote }}
    is_desktop = {{ $is_desktop }}
    is_laptop = {{ $is_laptop }}
